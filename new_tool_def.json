{
  "type": "tool",
  "definition": {
    "id": "system::echo",
    "description": "A simple tool that echoes back its input.",
    "parameters": {
      "type": "object",
      "properties": { "message": { "type": "string" } },
      "required": ["message"]
    },
    "outputSchema": {
      "type": "object",
      "properties": { "echo": { "type": "string" } }
    }
  }
}